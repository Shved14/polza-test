# Инструмент персонализации для email-аутрича

Инструмент автоматической персонализации компаний для холодного email-аутрича.

## Описание

Программа анализирует веб-сайты компаний и генерирует короткие персонализации для холодных email-рассылок на основе информации, найденной на сайтах.

## Технологический стек

- **Node.js** - runtime среда
- **TypeScript** - типобезопасность
- **npm** - менеджер пакетов
- **Google Gemini API** - генерация персонализаций
- **Axios** - HTTP запросы
- **Cheerio** - парсинг HTML
- **csv-parser** - чтение CSV
- **csv-writer** - запись CSV
- **p-limit** - контроль параллелизма
- **cli-progress** - индикатор прогресса
- **yargs** - аргументы CLI
- **zod** - валидация данных
- **ipaddr.js** - валидация IP-адресов

## Структура проекта

```
project/
├── README.md
├── package.json
├── tsconfig.json
├── .env.example
├── .gitignore
├── input/
│   └── companies.csv          # Входной файл с компаниями
├── output/
│   └── result.csv             # Выходной файл с персонализациями
└── src/
    ├── index.ts               # Точка входа
    ├── config/
    │   └── config.ts          # Конфигурация
    ├── types/
    │   └── index.ts           # TypeScript типы и интерфейс LLMProvider
    ├── services/
    │   ├── csv.service.ts     # Операции с CSV
    │   ├── web-scraping.service.ts  # Веб-скрапинг
    │   ├── text-cleaning.service.ts # Очистка текста
    │   └── gemini.provider.ts # Gemini API провайдер
    └── utils/
        └── logger.ts          # Логирование
```

## Установка

1. Клонируйте репозиторий
2. Установите зависимости:

```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

4. Отредактируйте `.env` и добавьте ваш API ключ Gemini:

```env
GEMINI_API_KEY=your_gemini_api_key_here
MODEL=gemini-pro-latest
LLM_MAX_TOKENS=200
LLM_TEMPERATURE=0.7
SCRAPING_TIMEOUT=30000
SCRAPING_MAX_RETRIES=3
SCRAPING_RETRY_DELAY=1000
SCRAPING_MAX_TEXT_LENGTH=6000
SCRAPING_CONCURRENCY=3
```

## Конфигурация

Переменные окружения:

- `GEMINI_API_KEY` - Ваш API ключ Gemini (обязательно)
- `MODEL` - Модель Gemini (по умолчанию: gemini-pro-latest)
- `LLM_MAX_TOKENS` - Максимальное количество токенов ответа (по умолчанию: 200)
- `LLM_TEMPERATURE` - Температура генерации (по умолчанию: 0.7)
- `SCRAPING_TIMEOUT` - Таймаут запроса в мс (по умолчанию: 30000)
- `SCRAPING_MAX_RETRIES` - Максимальное количество попыток (по умолчанию: 3)
- `SCRAPING_RETRY_DELAY` - Начальная задержка повтора в мс (по умолчанию: 1000)
- `SCRAPING_MAX_TEXT_LENGTH` - Максимальная длина текста перед отправкой в AI (по умолчанию: 6000)
- `SCRAPING_CONCURRENCY` - Количество параллельных запросов (по умолчанию: 3)

## Формат входного CSV файла

Создайте `input/companies.csv` со следующими колонками:

```csv
Компания,Сайт,Email
Пример Компания,https://example.com,contact@example.com
Тест ООО,https://test.ru,info@test.ru
```

**Важно:** Колонки должны называться точно: `Компания`, `Сайт`, `Email`.

## Запуск

### Режим разработки

```bash
npm run dev
```

### Сборка проекта

```bash
npm run build
```

### Запуск собранного проекта

```bash
npm start
```

### Опции CLI

```bash
npm start -- --input path/to/input.csv --output path/to/output.csv --debug
```

- `-i, --input` - Путь к входному CSV файлу
- `-o, --output` - Путь к выходному CSV файлу
- `-d, --debug` - Включить отладочное логирование

## Отладка

### Включение debug-режима

Для включения подробного логирования используйте флаг `--debug`:

```bash
npm start -- --debug
```

Или с указанием путей к файлам:

```bash
npm start -- --input ./input/companies.csv --output ./output/result.csv --debug
```

### Что показывает debug-режим

- Детализированные логи каждого шага обработки
- Информацию о HTTP запросах к сайтам
- Ответы от Gemini API
- Ошибки парсинга и очистки текста
- Статус retry-операций

### Логирование

Программа использует структурированное логирование с timestamp и уровнями:

```
[2024-01-01T12:00:00.000Z] [INFO] Starting email outreach personalization...
[2024-01-01T12:00:01.000Z] [INFO] Found 10 companies to process
Progress |████████████████████| 100% | 10/10 | ETA: 0s
[2024-01-01T12:00:30.000Z] [INFO] Processing complete. Results saved to: output/result.csv
```

### Типичные проблемы и решения

**Проблема:** Ошибка `GEMINI_API_KEY is not defined`

**Решение:** Убедитесь, что файл `.env` создан и содержит валидный API ключ.

**Проблема:** Таймауты при загрузке сайтов

**Решение:** Увеличьте `SCRAPING_TIMEOUT` в `.env` или уменьшите `SCRAPING_CONCURRENCY`.

**Проблема:** Слишком короткие персонализации

**Решение:** Увеличьте `LLM_MAX_TOKENS` в `.env` (рекомендуется 1024).

**Проблема:** Ошибки 403/SSL от сайтов

**Решение:** Это ожидаемо для некоторых сайтов. Скрипт продолжит обработку остальных компаний.

### Проверка конфигурации

Перед запуском проверьте, что:

1. Файл `.env` существует и содержит `GEMINI_API_KEY`
2. Входной CSV файл имеет правильный формат
3. Директории `input/` и `output/` существуют

## Формат выходного CSV файла

Результаты сохраняются в `output/result.csv` с колонками:

```csv
Компания,Сайт,Email,Персонализация
Пример Компания,https://example.com,contact@example.com,Заметил, что вы развиваете облачную ERP-систему для малого бизнеса.
Тест ООО,https://test.ru,info@test.ru,Unable to find reliable information for personalization.
```

## Как это работает

1. Программа читает CSV файл из `input/companies.csv` 
2. Для каждой компании:
   - Получает HTML сайта
   - Находит страницу "О компании" и извлекает текст
   - Находит раздел "Новости" или "Блог" и извлекает текст
   - Очищает HTML от скриптов, стилей, комментариев, рекламы
   - Обрезает текст до оптимального размера для AI
   - Отправляет информацию в Gemini API
   - Получает короткую персонализацию (максимум 40 слов, 2 предложения)
3. Записывает результаты в `output/result.csv` 

## Возможности

- **Параллельная обработка**: Обрабатывает несколько компаний одновременно с настраиваемым лимитом
- **Retry логика**: Экспоненциальный backoff для временных ошибок
- **Rate limiting**: Встроенный rate limiting для Gemini API
- **SSRF защита**: Валидация URL для предотвращения SSRF атак
- **Валидация входных данных**: Проверка CSV данных с помощью Zod схем
- **Прогресс-бар**: Визуальный индикатор прогресса с ETA
- **Graceful shutdown**: Обработка сигналов SIGINT/SIGTERM
- **Настраиваемость**: Все параметры настраиваются через переменные окружения
- **Обработка ошибок**: Комплексная обработка ошибок, продолжение работы при индивидуальных сбоях
- **Архитектура провайдеров**: Интерфейс LLMProvider позволяет легко добавлять другие LLM (OpenAI, Claude, DeepSeek)

## Обработка ошибок

Программа обрабатывает:

- Некорректные URL
- Отсутствующие сайты
- Таймауты соединения
- Ошибки 404
- Ошибки Gemini API (включая rate limits)
- Пустой HTML
- Сетевые ошибки
- Некорректные данные CSV

Если одна компания не обработалась, программа продолжает работу с остальными.

## Требования к персонализации

- Используется только информация, найденная на сайте
- Не придумываются факты и нет галлюцинаций
- Если информации недостаточно, возвращается: "Unable to find reliable information for personalization."
- Максимум 40 слов
- Максимум 2 предложения

## Пример персонализации

```
Заметил, что вы развиваете облачную ERP-систему для малого бизнеса и делаете упор на автоматизацию торговли.
```

## Безопасность

- Валидация URL предотвращает SSRF атаки
- Частные IP-адреса блокируются
- Localhost блокируется
- Metadata endpoints блокируются
- Разрешены только протоколы HTTP/HTTPS

## Лицензия

ISC

---

## Сдача тестового задания — Polza Agency

### Что у тебя уже есть

| Задача | Статус | Где лежит |
|--------|--------|-----------|
| 1. База ≥50 компаний | Готово | `submission/1_база_клиентов.csv` (из `base.csv`) |
| 2. Скрипт персонализации | Готово | этот репозиторий (`src/`, запуск `npm start`) |
| 3. Цепочка из 3 писем | Готово | `submission/3_цепочка_писем.md` |
| 4. Персонализация 15 компаний | Частично | `submission/4_персонализация_тест.csv` |

### Как сдать (по пунктам ТЗ)

#### Задача 1 — Google Таблица

1. Открой [Google Sheets](https://sheets.google.com) → Файл → Импорт → загрузи `1_база_клиентов.csv`.
2. Проверь столбцы: **Компания, Сайт, Email, Имя, Должность, Заметки**.
3. Включи доступ «Все, у кого есть ссылка» → скопируй ссылку.

#### Задача 2 — Скрипт + персонализация базы

1. Выложи проект на GitHub (без `.env` и ключей!) и приложи ссылку на репозиторий.
2. *(Опционально, но лучше)* прогони скрипт по базе из задачи 1 и добавь столбец «Персонализация» в ту же Google Таблицу отдельным столбцом или листом.

```bash
# пример: скопировать базу в input и запустить
# (приведи CSV к формату: Компания,Сайт,Email)
npm run build
npm start -- --input ./input/companies.csv --output ./output/result.csv
```

#### Задача 3 — Цепочка писем

1. Создай в той же Google Таблице **второй лист** «Цепочка писем».
2. Или приложи документ: скопируй текст из `3_цепочка_писем.md` в Google Docs.
3. В письме 1 должна быть переменная `{{персонализация}}`.

#### Задача 4 — Тестовая база (15 компаний)

1. Импортируй `4_персонализация_тест.csv` в Google Sheets / приложи CSV или XLSX.
2. В комментарии к сдаче кратко уккажи: часть строк с ошибками — сайты 403/SSL/таймаут или перепутанные пары «компания↔сайт» (это как раз кейс из ТЗ «вдруг мы что-то перепутали»).

### Что приложить в ответе проверяющему

1. Ссылка на Google Таблицу (база 50+ и/или персонализация).
2. Ссылка на GitHub-репозиторий со скриптом.
3. Цепочка писем (лист таблицы или Google Doc).
4. CSV/таблица по задаче 4 (15 компаний + персонализация).

### Важно перед сдачей

- В репозитории **нет** `.env` и API-ключей
- README объясняет `npm install` → `.env` → `npm run build` → `npm start` 
- В письме 1 есть `{{персонализация}}` 
- База ≥ 50 строк (сейчас 50 компаний)
- Перепрогони задачу 4, если хочешь меньше «обрезанных» ответов (после фикса `LLM_MAX_TOKENS=1024`)

---

## Текст для сдачи проверяющему (можно вставить в форму / Telegram / Notion)

### Кратко

Сделал тестовое по Polza Agency: собрал базу B2B-компаний, написал скрипт персонализации на Cursor (TypeScript + Gemini), подготовил цепочку из 3 писем и прогнал персонализацию по тестовой базе из 15 компаний.

### Задача 1 — База клиентов

- Файл / таблица: **[ВСТАВЬ ССЫЛКУ НА GOOGLE SHEETS]**
- Источник: открытые данные / сайты компаний (B2B, Россия, IT/SaaS/промышленность/логистика)
- ≥ 50 компаний: название, сайт, email, имя ЛПР (где удалось найти), должность

### Задача 2 — Скрипт персонализации

- Репозиторий: **[ВСТАВЬ ССЫЛКУ НА GITHUB]**
- Стек: Node.js, TypeScript, Cheerio (парсинг сайта), Google Gemini API
- Вход: CSV `Компания,Сайт,Email` 
- Выход: CSV со столбцом `Персонализация` 
- Запуск: `npm install` → `.env` с `GEMINI_API_KEY` → `npm run build` → `npm start` 

### Задача 3 — Цепочка писем

- Документ / лист таблицы: **[ВСТАВЬ ССЫЛКУ]**
- Письмо 1 использует переменную `{{персонализация}}` 
- Письмо 2 — фолоу-ап через 3 дня (кейс)
- Письмо 3 — финальный мягкий выход через 5 дней

### Задача 4 — Персонализация тестовой базы

- Файл: **[ВСТАВЬ ССЫЛКУ НА CSV/GOOGLE SHEETS]**
- Скрипт тот же, что в задаче 2
- Часть строк с ошибками (403, SSL, таймаут) или с расхождением «название компании ≠ сайт» — это ожидаемо по формулировке ТЗ («вдруг мы что-то перепутали»): скрипт персонализирует **сайт**, а не выдумывает факты под имя из CSV

---

Спасибо!
